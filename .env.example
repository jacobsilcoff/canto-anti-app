# Get a free API key at https://console.groq.com (no credit card required)
GROQ_API_KEY=your_key_here

# Path to SQLite database (default: data/cards.db)
# On Render with a persistent disk mounted at /var/data, use:
# DB_PATH=/var/data/cards.db
DB_PATH=data/cards.db
